cloud:
  aws:
    credentials:
      access-key: ${{ secrets.ACCESSKEY }}
      secret-key: ${{ secrets.ACCESSKEY }}
      
    region:
      static: ap-south-1
      auto: false
      
    stack:
      auto: false
      

     
      
